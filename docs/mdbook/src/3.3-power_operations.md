# Power operations

A Node may be rebooted or powered off using the `Reboot` and `Shutdown` operations, respectively.

To initiate a power operation, patch the Node with the following:

```json
{"operation": {"type": "Reboot"}}
```

or

```json
{"operation": {"type": "Shutdown"}}
```
