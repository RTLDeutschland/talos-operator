{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "talos-operator.fullname" . }}-allow-metrics-traffic
  labels:
    {{- include "talos-operator.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "talos-operator.selectorLabels" . | nindent 6 }}
      control-plane: controller-manager
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              metrics: enabled
      ports:
        - port: 8443
          protocol: TCP
{{- end }}
